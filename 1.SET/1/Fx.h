#ifndef FX_H
#define FX_H

#include <iostream>
#include <vector>
#include <algorithm>
#include <sstream>

using namespace std;


auto draw(auto  a);
auto get_data();
auto union_sets(auto  a, auto  b);
auto intersections(auto a, auto b);
auto difference(auto a, auto b);



#endif